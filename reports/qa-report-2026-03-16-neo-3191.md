# QA Report: NEO-3191 -- Add `ean` field to Search API finishes array

**Date:** 2026-03-16
**Status:** PASS
**Repo:** grohe-neo-services
**Branch:** feature/NEO-3191

## Summary

The implementation adds the `ean` field to the Search API response's `finishes[]` array.
Three files were modified. All changes are correct, consistent with existing patterns, and
complete. All 50 SearchApi tests pass (including 3 new tests for the Ean mapping).

## Code Review

### 1. `ProductResultFinish.cs` -- Model change
- Added `public long Ean { get; set; }` with `[JsonPropertyName("ean")]`
- Type matches source model (`IndexProductFieldsVariant.Ean` is `long`)
- JSON attribute follows the lowercase convention used by all other properties
- Placed after `Sku` property, matching the field order in the source model
- XML doc comment included ("European Article Number (EAN)")
- **Verdict:** Correct

### 2. `SearchResultsMapper.cs` -- Mapping change
- Added `model.Ean = finish.Ean;` at line 777, between `model.Sku` and `model.Name`
- Direct property-to-property copy, matching the pattern of all other fields
- `FinishDefinition` inherits from `IndexProductFieldsVariant`, so `finish.Ean` is available
- No other mappers in the SearchApi project map `FinishDefinition` -- this is the only place
- **Verdict:** Correct

### 3. `SearchResultsMapperEanTests.cs` -- New tests
Three new test methods added:

| Test | Purpose | Verdict |
|---|---|---|
| `Map_FinishDefinition_MapsEanToProductResultFinish` | Verifies `Map()` copies all fields including Ean | Good -- comprehensive, asserts all properties |
| `Map_FinishDefinition_ReturnsNull_WhenFinishIsNull` | Null guard on the `Map()` method | Good -- covers null input edge case |
| `GetOrderedFinishes_MapsEanToProductResultFinish` | End-to-end: Ean survives the ordering pipeline | Good -- validates Ean persists through GetOrderedFinishes |

All three follow the `[Method]_[Scenario]_[ExpectedBehavior]` naming convention.
Tests use the existing `CreateMapper()` factory and `CreateFinish()` helper.

## Test Run Results

```
Test Run Successful.
Total tests: 50
     Passed: 50
 Total time: 0.64 Seconds
```

All 50 SearchApi tests passed -- 0 failures, 0 skipped.

## Coverage Analysis

The `Ean` property is a non-nullable `long` (defaults to 0). The existing pre-change tests
that create `FinishDefinition` objects via `CreateFinish()` already supply an `ean` parameter,
so all existing EAN ordering tests implicitly exercise the mapping. The 3 new tests explicitly
verify the Ean value propagates correctly through `Map()` and `GetOrderedFinishes()`.

No additional test gaps identified.

## Completeness Check

- [x] `ProductResultFinish.Ean` type matches source (`long`)
- [x] JSON serialization attribute correct (`"ean"`)
- [x] Mapper copies the field
- [x] No other mappers need the same change (only `SearchResultsMapper` maps finishes)
- [x] Tests follow existing conventions
- [x] All tests pass
- [x] No source code modified outside test files

## Bugs Found

None.

## Recommendations

None -- the implementation is clean and complete.
